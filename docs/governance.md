# Governance

This document defines lightweight governance for delivery quality.

## Required Controls

- Every pull request must pass CI checks.
- Service packages must provide `lint` and `typecheck` scripts.
- Required architecture/governance/release docs must exist and remain current.
- Production releases must follow the documented release checklist.
